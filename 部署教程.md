# 爆款素材工作台 - 部署教程

## 你拿到了什么

一个每天自动抓取 B站 7 位博主（钱婧、大冰、张雪峰等）高赞内容、
AI 生成摘要和创作灵感的网页工作台。

## 永久部署到 GitHub Pages（5分钟）

### 第1步：下载并解压项目包

下载 `爆款素材工作台-完整项目.zip`，解压到一个文件夹。

### 第2步：登录 GitHub

打开 https://github.com 登录你的账号。
（如果登不上，常见原因：①新账号需要先验证邮箱 ②密码要求8位以上含数字 ③可能需要科学上网）

### 第3步：创建新仓库

1. 点右上角 **+** → **New repository**
2. 仓库名填：`爆款素材工作台`（或英文名 `content-workbench`）
3. 选 **Public**（公开）
4. **不要**勾选任何初始化文件（README、.gitignore 等）
5. 点 **Create repository**

### 第4步：上传文件

**方式一（推荐，最简单）：网页拖拽上传**

1. 在刚创建的空仓库页面，点 **uploading an existing file**
2. 把解压后的**所有文件和文件夹**拖进去
3. 注意：`.github` 文件夹和 `.gitignore` 是隐藏文件
   - Mac: 按 `Cmd + Shift + .` 显示隐藏文件
   - Windows: 资源管理器 → 查看 → 勾选"隐藏的项目"
4. 拖完后点 **Commit changes**

**方式二：用 Git 命令（如果你会用）**

```bash
cd 解压后的文件夹
git init
git add .
git commit -m "初始提交"
git branch -M main
git remote add origin https://github.com/你的用户名/仓库名.git
git push -u origin main
```

### 第5步：开启 GitHub Pages

1. 进入仓库 → **Settings** → 左侧 **Pages**
2. **Source** 选 **GitHub Actions**
3. 保存

### 第6步：手动触发一次

1. 进入仓库 → **Actions** 标签页
2. 左侧选 **每日爆款抓取**
3. 点 **Run workflow** → **Run workflow**
4. 等 2-3 分钟跑完

### 第7步：拿到永久链接

跑完后，回到 **Settings → Pages**，
顶部会显示：

```
Your site is live at https://你的用户名.github.io/仓库名/
```

**这就是你的永久链接！** 手机打开即可，每天自动更新。

---

## 自动更新机制

- **每天北京时间 8:00 自动执行**：抓取最新内容 → AI 总结 → 自动部署
- 链接永远不变，内容每天自动刷新
- 想手动更新：Actions 页 → 每日爆款抓取 → Run workflow

## 本地运行（可选）

```bash
pip install -r requirements.txt
python run.py          # 抓取+总结+生成HTML
```
生成的 `工作台.html` 浏览器打开即可。

## 项目结构

```
├── .github/workflows/daily.yml  # GitHub Actions 自动更新配置
├── fetch_daily.py               # B站抓取脚本
├── ai_summarize.py              # AI 总结模块
├── build_app.py                 # HTML 生成器
├── run.py                       # 一键运行入口
├── requirements.txt             # Python 依赖
├── index.html                   # 工作台页面（GitHub Pages 首页）
├── 工作台.html                   # 工作台页面（中文名）
└── dashboard.html               # 工作台页面（英文名）
```
